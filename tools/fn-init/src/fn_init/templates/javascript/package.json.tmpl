{
  "name": "{{FUNCTION_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm --prefix {{SDK_PATH}} install && npm --prefix {{SDK_PATH}} run build",
    "pretest": "npm --prefix {{SDK_PATH}} install && npm --prefix {{SDK_PATH}} run build",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "nanofaas-function-sdk": "file:{{SDK_PATH}}"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
