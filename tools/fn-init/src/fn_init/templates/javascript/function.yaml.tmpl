name: {{FUNCTION_NAME}}
image: {{IMAGE_TAG}}
timeoutMs: 10000
concurrency: 2
executionMode: DEPLOYMENT

x-cli:
  build:
    context: {{BUILD_CONTEXT}}
    dockerfile: {{DOCKERFILE_PATH}}
    platform: linux/amd64
    push: true
