apiVersion: apps/v1
kind: Deployment
metadata:
  name: nanofaas-control-plane
  namespace: nanofaas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nanofaas-control-plane
  template:
    metadata:
      labels:
        app: nanofaas-control-plane
    spec:
      serviceAccountName: nanofaas-control-plane
      containers:
        - name: control-plane
          image: nanofaas/control-plane:0.5.0
          ports:
            - containerPort: 8080
            - containerPort: 8081
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
