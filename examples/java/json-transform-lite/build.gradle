plugins {
    id 'application'
    id 'org.graalvm.buildtools.native' version "${graalvmBuildToolsVersion}"
}

dependencies {
    implementation project(':function-sdk-java-lite')
}

application {
    mainClass = 'it.unimib.datai.nanofaas.examples.jsontransformlite.JsonTransformLite'
}

tasks.named('test') {
    useJUnitPlatform()
}

graalvmNative {
    binaries {
        main {
            imageName = 'json-transform-lite'
            mainClass = 'it.unimib.datai.nanofaas.examples.jsontransformlite.JsonTransformLite'
        }
    }
}
